============
Installation
============

At the command line::

    $ easy_install django-bloxby

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-bloxby
    $ pip install django-bloxby
